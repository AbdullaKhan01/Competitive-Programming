#include<bits/stdc++.h>
#define mod 1000000007
#define mod2 998244353
#define ceil(a,b) ((a+b-1)/b)
#define ll long long
#define lli long long int
#define vi vector<int>
#define vll vector<ll>
#define all(x) (x).begin(),(x).end()
#define rall(x) (x).rbegin(),(x).rend()
#define input(x) for(auto &a:x) cin >> a;
#define llmax(a,b) max(1LL*(a),1LL*(b))
#define output(x) for(auto a:x) cout << a <<" ";
#define FOR(i,n) for(i=0;i<n;i++)
using namespace std; 
// bool solve(vll &arr,ll n,ll mid){
	
// }
int main(){
	cout << "OK" << endl;
    // ll n;
    // cin >> n;
    // ll m;
    // cin >> m;
    // vll arr(n);
    // input(arr);
    // ll tot = 0;
    // for(auto s:arr) tot+=s;
    // ll z = *max_element(all(arr));
	// ll s = 0 , e = z;
	// ll mid = (s+e)/2;
	// ll mini;
	// while(s<=e){
	// 	if(solve(arr,n,mid)){
	// 		mini = mid;
	// 		e = mid-1;
	// 	}
	// 	else{
	// 		s = mid+1;
	// 	}
	// 	mid = (s+e)/2;
	// }
	// ll maxi = z+m;
	// cout << mini << " " << maxi << endl;
    return 0;
}